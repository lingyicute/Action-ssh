name: CI
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: chmod +x ./setup-ssh
      - run: $GITHUB_WORKSPACE/setup-ssh
        shell: bash
