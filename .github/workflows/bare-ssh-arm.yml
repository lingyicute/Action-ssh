name: bare SSH Github-runner ARM

# 这些 workflow 只需要读代码，显式收窄 GITHUB_TOKEN 权限。
permissions:
  contents: read

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-linux-ssh.yml
    with:
      runs-on: ubuntu-26.04-arm
